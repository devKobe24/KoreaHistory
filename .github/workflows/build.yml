name: build-and-deploy

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Build jar
        run: ./gradlew clean bootJar

      - name: Upload jar artifact
        uses: actions/upload-artifact@v4
        with:
          name: a-tti
          path: build/libs/*.jar

  deploy:
    needs: build
    runs-on: self-hosted

    steps:
      - name: Download jar artifact
        uses: actions/download-artifact@v4
        with:
          name: a-tti
          path: ./artifacts

      - name: Detect jar filename
        run: |
          echo "Working directory: $(pwd)"
          echo "Checking artifacts directory:"
          ls -la ./artifacts/ || echo "Artifacts directory not found"
          JAR_FILE=$(ls ./artifacts/*.jar 2>/dev/null | head -n 1 | xargs basename)
          if [ -z "$JAR_FILE" ]; then
            echo "Error: No JAR file found in artifacts directory"
            exit 1
          fi
          echo "JAR_NAME=$JAR_FILE" >> $GITHUB_ENV
          echo "✅ Detected JAR file: $JAR_FILE"

      - name: Copy jar to deploy directory
        run: |
          echo "Copying artifacts/$JAR_NAME to /opt/deploy/a-tti/"
          cp -v "./artifacts/$JAR_NAME" "/opt/deploy/a-tti/$JAR_NAME"
          echo "File copy completed"

      - name: Run deploy script
        run: |
          echo "Running deploy script with $JAR_NAME"
          cd /opt/deploy/a-tti && ./deploy.sh "$JAR_NAME"
          echo "Deploy script completed"
